{
    "name": "js-playground",
    "version": "1.0.0",
    "type": "commonjs",
    "private": true,
    "scripts": {
        "build": "mkdir -p dist && cp -r src/* dist/",
        "start": "npx serve dist -p $PORT",
        "dev": "npx serve src -p 3000"
    },
    "dependencies": {
        "serve": "^14.2.1"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
